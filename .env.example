# Plantilla de variables de entorno. Copiar a .env y completar los valores.
# En producción (Render) estas variables se cargan desde el dashboard, no desde .env.

# IA — OpenRouter (gateway compatible con OpenAI)
OPENROUTER_API_KEY=
